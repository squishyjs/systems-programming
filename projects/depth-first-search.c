#include <stdio.h>
#include <stdlib.h>
#include <time.h>
#include <unistd.h>
#include <string.h>

struct Graph {

};

typedef struct Vertex {

} Vertex;


void depth_fist_search(struct Graph, Vertex) {
    sleep(1);
    puts("Starting DFS algorithm..");
}

int main(int argc, char *argv[]) { return 0; }
